{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cmake",
			"label": "CMake: build tests",
			"command": "build",
			"targets": [
				"all"
			],
			"group": "build",
			"problemMatcher": [],
			"options": { "cwd": "${workspaceFolder}/build"},
			"detail": "CMake template build task",
		}
	]
}
